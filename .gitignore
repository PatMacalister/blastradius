node_modules/
*.log
.DS_Store

# Changelog scanner state — snapshots of provider changelogs for diffing.
tools/.changelog-state/

# Never commit anything that could hold a real credential.
.env
.env.*
!.env.example
*.report.json

# Live contract-test credentials for the VPS deployment. The whole point of this project is
# that a credential in the working tree is reachable by anything that goes looking.
ops/*.env
!ops/*.env.example

# Patrick's working notes. Removed from history on 2026-07-28 before the repo went public:
# it carried fingerprints of live credentials and the file:line of each in the credential
# store, which is a reconnaissance map in a public repo. Kept locally, never tracked.
TODO-PATRICK.md
